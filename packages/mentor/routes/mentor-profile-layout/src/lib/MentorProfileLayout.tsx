import Provider from '../provider.template';

/* eslint-disable-next-line */
export interface MentorProfileLayoutProps {}

export function MentorProfileLayout(props: MentorProfileLayoutProps) {
  return (
    <div>
      <Provider appId="MENTOR_APP1_ID3" />
    </div>
  );
}

export default MentorProfileLayout;
