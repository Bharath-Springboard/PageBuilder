/* eslint-disable-next-line */
export interface StudentProgressProps {}

export function StudentProgress(props: StudentProgressProps) {
  return (
    <div>
      <h1>Welcome to StudentProgress!</h1>
    </div>
  );
}

export default StudentProgress;
